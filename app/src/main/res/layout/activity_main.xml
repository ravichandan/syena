<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="apps.chans.com.syena.MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:id="@+id/mainToolbarLayout">

        <android.support.v7.widget.Toolbar
            android:id="@+id/mainToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            android:elevation="4sp"
            android:popupTheme="@style/ThemeOverlay.AppCompat.Light"
            android:theme="@style/ThemeOverlay.AppCompat.ActionBar"

            ></android.support.v7.widget.Toolbar>

        <ImageView
            android:id="@+id/tagMemberIV"
            android:layout_width="60sp"
            android:layout_height="60sp"
            android:adjustViewBounds="true"
            android:foregroundGravity="bottom|right"
            android:onClick="tagMember"
            android:src="@mipmap/eagle_2"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true" />

        <ExpandableListView
            android:id="@+id/expandableListView"
            style="@style/Widget.AppCompat.ListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:footerDividersEnabled="true"
            android:headerDividersEnabled="true"
            android:indicatorLeft="?android:attr/expandableListPreferredItemIndicatorLeft"
            android:layout_above="@+id/tagMemberIV"
            android:layout_alignParentStart="true" />
    </RelativeLayout>

    <!--<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="@+id/mainToolbarLayout">

    <ExpandableListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/expandableListView"
            style="@style/Widget.AppCompat.ListView"
            android:layout_marginBottom="56dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:stackFromBottom="false"></ExpandableListView>

    </RelativeLayout>-->
</android.support.v4.widget.SwipeRefreshLayout>
